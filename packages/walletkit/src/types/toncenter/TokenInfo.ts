export interface TokenInfo {
    description?: string;
    extra?: { [key: string]: never };
    image?: string;
    name?: string;
    nftIndex?: string;
    symbol?: string;
    type?: string;
    valid?: boolean;
}
